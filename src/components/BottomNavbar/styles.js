import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    width: '100%',
    position: 'fixed',
    bottom: 0,
    paddingTop: '10px',
    paddingBottom: '10px',
    backgroundColor: '#232b36',
  },
}));